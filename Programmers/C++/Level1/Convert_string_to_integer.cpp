#include <string>
#include <vector>

using namespace std;

int solution(string s) {
    return stoi(s); // stoi함수는 string을 int로 변환해주는 함수이다.
}

// C언어에서의 atoi함수와 같은 기능이다.
// ( C언어 : atoi / C++ : stoi ) 